/**
 * En esta seccion veremos como seleccionar elementos por medio de ID
 * 
 * Recordemos que los ID se usan solo una vez por elemento en el documento
 * Veamos unos ejemplos!
 * 
 * 
 */

const formulario=document.getElementById("formulario");

console.log(formulario);


/**
 * En este caso si le pedimos un elemento que no existe nos devolbera 
 * un null
 */

// const noExiste=document.getElementById("no-existe");

// console.log(noExiste);
