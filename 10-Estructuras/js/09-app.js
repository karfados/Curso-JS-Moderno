/**
 * En esta seccion veremos el operador ternario.
 * El operador tenerio es una forma de abreviar el los if, por 
 * medio de una sintaxis mas sensilla.
 */

const autenticado=false;

console.log(autenticado?"Si estas autentificado":"No estas autentificado");