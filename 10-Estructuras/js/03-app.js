/**
 * En esta seccuion veremos ejemplos de como se usa el comparador 
 * mayoy o igual y el comparador menor o igual.
 */

const dinero=300; //Modifica el valor del dinero para jugar con el codigo!
const totalAPagar=300;

if (dinero>=totalAPagar) {
    console.log("Si podemos pagar!!");
} else {
    console.log("No podemos pagar!");
}